<!DOCTYPE html>
<html ng-app='eShop'>
	<head>
		<title>E-Shop</title>
		<%= stylesheet_link_tag    "application", :media => "all" %>
		<%= javascript_include_tag "application" %>
		<%= csrf_meta_tags %>
	</head>
	<body>
		<%= render 'shared/header' %>
		<div class="container">
			 <div class="row">
			 	<div class="col-md-3">
			 		<%= render 'shared/siderbar' %>
			 	</div>
			 	<div class="col-md-9" id="content" ng-view>
			 		<%= yield %>	
			 	</div>
			</div>
		</div>
		<%= render 'shared/footer' %>
	</body>
</html>
